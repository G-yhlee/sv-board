<!-- <script>
	let board = Array(19).fill(Array(19).fill(null));
	let currentPlayer = 'black';
  
	function placeStone(row, col) {
	  // Add logic to place stone and check for captures and illegal moves
	  // Update the board and currentPlayer variables accordingly
	}
  </script>
  
  <table>
	{#each board as row, rowIndex}
	  <tr>
		{#each row as cell, colIndex}
		  <td on:click={() => placeStone(rowIndex, colIndex)}></td>
		{/each}
	  </tr>
	{/each}
  </table> -->